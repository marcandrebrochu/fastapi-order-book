[project]
name = "fastapi-market-sim"
version = "0.0.1"
description = "Basic market simulator and an API for it"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.136.3",
]

[tool.fastapi]
entrypoint = "app.main:app"